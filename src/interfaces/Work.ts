export interface IWorkCard {
  id: string;
  time: string;
  title: string;
  timeline: string;
  budget: string;
  skillLevel: string;
  content: string;
  tags: string[];
  proposals: string;
}
[];

export interface ITags {
  tag: string;
}
